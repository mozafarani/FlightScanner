import React from "react";

const Header = () => {
  return (
    <header>
      <h1>Flight Finder</h1>
      <nav>{/* Add navigation links here */}</nav>
    </header>
  );
};

export default Header;
